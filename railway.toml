[build]
builder = "dockerfile"
dockerfilePath = "./Dockerfile"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
port = "${{PORT}}"

[service.healthcheck]
path = "/health"
interval = 30
timeout = 5
